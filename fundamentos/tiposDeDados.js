// Tipos de dados primitivos

const nome = 'Jonathan'; // String
const nome1 = 'Jonathan Lopes'; // String

const num1 = 10; // Number
const num2 = 10.52; // Number 

let nomeAluno; // Undefined -> Não aponta pra local nenhum na memória
let sobrenomeAluno = null; // Nulo -> Não aponta pra local nenhum na memória

const aprovado = true; // Boolean = verdadeiro ou falso
const reprovado = false;

console.log(typeof nome); // typeof é utilizado para verificar o tipo da variável. Se quiser verificar o tipo e o valor basta colocar  (typeof nome, nome)